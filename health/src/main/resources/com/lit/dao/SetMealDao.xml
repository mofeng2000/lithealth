<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lit.dao.SetMealDao">
    <!--新增套餐-->
    <!--    <insert id="add" parameterType="com.lit.pojo.CheckGroup" useGeneratedKeys="true" keyProperty="id">-->
    <insert id="add" parameterType="com.lit.pojo.Setmeal">
        -- 获取自增的id值
        <selectKey resultType="java.lang.Integer" order="AFTER" keyProperty="id">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into t_setmeal(code,name,sex,age,help_code,price,remark,attention,img)
        values
        (#{code},#{name},#{sex},#{age},#{helpCode},#{price},#{remark},#{attention},#{img})
    </insert>
    <!--设置套餐和检查组的关联关系-->
    <insert id="setMealAndCheckGroup" parameterType="hashmap">
        insert into t_setmeal_checkgroup(setmeal_id, checkgroup_id)
        values (#{setMealId}, #{checkGroupIds})
    </insert>
    <!--根据套餐id查询关联的检查组id-->
    <select id="findCheckGroupIdBySetMealId" parameterType="int" resultType="int">
        select checkgroup_id from t_setmeal_checkgroup where setmeal_id=#{id};
    </select>
    <!--删除检查组id和关联的套餐id-->
    <delete id="deleteAssociation" parameterType="int">
        delete from t_setmeal_checkgroup where setmeal_id=#{id};
    </delete>
    <select id="findSetmealCount" resultType="map">
        select s.name,count(o.id) as value
        from t_order o ,t_setmeal s
        where o.setmeal_id = s.id
        group by s.name
    </select>
</mapper>