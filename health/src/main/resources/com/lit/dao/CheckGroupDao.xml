<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lit.dao.CheckGroupDao">

    <!--新增-->
    <!--    <insert id="add" parameterType="com.lit.pojo.CheckGroup" useGeneratedKeys="true" keyProperty="id">-->
    <insert id="add" parameterType="com.lit.pojo.CheckGroup">
        -- 获取自增的id值
        <selectKey resultType="java.lang.Integer" order="AFTER" keyProperty="id">
            SELECT LAST_INSERT_ID()
        </selectKey>

        insert into t_check_group(code,name,sex,help_code,remark,attention)
        values
        (#{code},#{name},#{sex},#{helpCode},#{remark},#{attention})
    </insert>
    <!--设置检查组和检查项的关联关系-->
    <insert id="setCheckGroupAndCheckItem" parameterType="hashmap">
        insert into t_checkgroup_checkitem(checkgroup_id, checkitem_id)
        values (#{checkGroupId}, #{checkitemId})
    </insert>
    <!--根据检查组id查询关联的检查项id-->
    <select id="findCheckItemIdsByCheckGroupId" parameterType="int" resultType="int">
        select checkitem_id from t_checkgroup_checkitem where checkgroup_id=#{id};
    </select>
    <!--删除检查组id和关联的检查项id-->
    <delete id="deleteAssociation" parameterType="int">
        delete from t_checkgroup_checkitem where checkgroup_id=#{id};
    </delete>

</mapper>