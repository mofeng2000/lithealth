<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lit.dao.OrderSettingDao" >
    <insert id="add" parameterType="com.lit.pojo.OrderSetting">
        insert into t_ordersetting (order_date, number, reservations)
        values (#{orderDate}, #{number}, #{reservations})
    </insert>
    <!--根据日期更新预约人数-->
    <update id="editNumberByOrderDate" parameterType="com.lit.pojo.OrderSetting">
        update t_ordersetting
        set number = #{number}
        where order_date = #{orderDate}
    </update>
    <!--根据预约日期查询-->
    <select id="findCountByOrderDate" parameterType="java.util.Date" resultType="long">
        select count(*)
        from t_ordersetting
        where order_date = #{orderDate}
    </select>
    <!--根据月份查询预约设置信息-->
    <select id="getOrderSettingByMonth"
            parameterType="hashmap"
            resultType="com.lit.pojo.OrderSetting">
        select * from t_ordersetting where order_date between #{dateBegin} and #{dateEnd}
    </select>
</mapper>